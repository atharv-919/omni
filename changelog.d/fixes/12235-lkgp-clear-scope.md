- **fix(resilience):** clear a combo's Last-Known-Good-Provider pin only when it actually names the target that just failed — the target-scoped `executionKey` pin is still always cleared, but the combo-level pin records whichever provider last *succeeded*, and under `auto` it is a scoring input (`resolveAutoStrategy` reads it into `lastKnownGoodProvider`), so clearing it every time an unrelated target was skipped discarded a preference for a provider that never failed; a sibling connection failing no longer invalidates a pin naming a different connection, and a caller with no failed target in scope keeps the previous unconditional behaviour ([#12235](https://github.com/diegosouzapw/OmniRoute/issues/12235)) (#12235)
