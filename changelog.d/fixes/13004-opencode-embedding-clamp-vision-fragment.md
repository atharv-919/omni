- **fix(opencode):** clamp oversized embedding string inputs to 20k chars before they reach the upstream body (behavioural fix — the `clampEmbeddingStringInput` helper existed but was never wired into `buildUpstreamBody`/CLOVA single-text dispatch, so over-limit batches still hit models that reject them), and preserve the GLM-5.3 vision fragment plus reasoning-presence sentinel for opencode/zen relay turns ([#13004](https://github.com/diegosouzapw/OmniRoute/pull/13004)) — thanks @alvinveroy
