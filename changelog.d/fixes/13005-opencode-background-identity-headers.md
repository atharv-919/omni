- **fix(opencode):** send the CLI identity headers and `x-opencode-session` on background calls (model-catalog discovery, quota fetcher) that previously bypassed `buildHeaders()` and went out with a bare `Bun fetch` user agent — the session id is a stable per-connection fingerprint seeded by any of the workspace keys the `providerSpecificData` validator accepts (`openCodeGoWorkspaceId`/`opencodeGoWorkspaceId`/`workspaceId`), falling back to the connection id, so background traffic groups under one upstream identity instead of a fresh anonymous UUID per call ([#13005](https://github.com/diegosouzapw/OmniRoute/pull/13005)) — thanks @alvinveroy
